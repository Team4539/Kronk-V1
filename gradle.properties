## Fix for Gradle daemon connection failure when Tailscale VPN is active.
## Tailscale adds network interfaces that confuse Gradle's daemon address resolution.
## This forces Gradle to prefer IPv4 and only use localhost for daemon communication.
org.gradle.jvmargs=-Xmx512m -Djava.net.preferIPv4Stack=true
